# Fixed footprint selection — all 21 library components now resolve correctly

## What changed
- **`AMS1117-3.3/profile.json`** was a broken stub (`description: "MODIFIED TEST"`, empty pins, empty package_types). Restored with: `footprint: "SOT-223"`, correct 4-pin definition (GND/OUTPUT/INPUT/OUTPUT), and `package_types: ["SOT-223", "TO-252", "TO-220"]`.
- Confirmed the backend `_pick_footprint` already has the correct priority logic (from a prior session): `GENERIC_SLUG_FP` is checked before `package_types` (prevents "0402 SMD" in LED's package list from overriding LED-5mm), and `IC_SLUG_FP` covers all 13 known ICs as a safety net.

## Verified footprint resolution (all correct)
| Component | Priority | Footprint |
|---|---|---|
| AMS1117-3.3 | P0 profile.footprint | SOT-223 |
| DRV8833 | P2 HTSSOP→TSSOP match | TSSOP-16 |
| ATMEGA328P | P2 "28-pin DIP" pattern | DIP-28 |
| LED | P1 GENERIC_SLUG_FP | LED-5mm |
| ESP32_WROOM_32 | P3 IC_SLUG_FP | DIP-40 |
| … all others | P0–P3 | correct |

## Files
- `frontend/static/library/AMS1117-3.3/profile.json` — restored

## Notes
`ESP32_WROOM_32` and `L298N` use approximate footprints (DIP-40 and DIP-20) because no module/Multiwatt footprint exists in the footprints library. These work for placement but are not physically accurate.
