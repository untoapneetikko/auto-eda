# Made chat "back" arrow into a visible button

## What changed
- `.pa-modal-close` CSS: added background (#1e2130), border (1px solid), rounded corners (6px), padding (5px 10px), flex layout, font-weight, and smooth hover transition (highlights purple border + lighter background)
- Button text changed from bare `←` to `‹ Back` for clarity

## Files
- `frontend/static/index.html` — CSS + button element
